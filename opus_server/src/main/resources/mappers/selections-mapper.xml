<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="nknk.opus.project.selections.model.mapper.SelectionsMapper">

	<select id="selectGoodsList">
		SELECT GOODS_NO, GOODS_NAME, GOODS_SORT, GOODS_CATEGORY, GOODS_PRICE, GOODS_IMG_PATH, GOODS_IMG_RE 
		FROM GOODS LEFT JOIN GOODS_IMG USING (GOODS_NO)
		WHERE GOODS_DEL_FL = 'N'
		AND GOODS_IMG_ORDER = 0
	</select>
	
	<select id="selectGoodsDetail">
		SELECT GOODS_NO, GOODS_NAME, GOODS_SORT, GOODS_CATEGORY, GOODS_PRICE, GOODS_INFO, DELIVERY_COST, GOODS_SELLER,
		GOODS_IMG_PATH, GOODS_IMG_RE 
		FROM GOODS LEFT JOIN GOODS_IMG USING (GOODS_NO)
		WHERE GOODS_NO = ${goodsNo} 
		AND GOODS_DEL_FL = 'N'
		AND GOODS_IMG_ORDER = 0
	</select>
	
	<select id="selectGoodsOptions">
		SELECT GOODS_OPTION_NO, GOODS_SIZE, GOODS_COLOR, STOCK, OPTION_PRICE
		FROM GOODS_OPTION
		WHERE GOODS_NO = ${goodsNo}
	</select>
	
	<select id="selectGoodsImgList">
		SELECT GOODS_IMG_NO, GOODS_IMG_PATH, GOODS_IMG_RE, GOODS_IMG_ORDER
		FROM GOODS_IMG
		WHERE GOODS_NO = ${goodsNo}
	</select>

</mapper>
