<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="nknk.opus.project.bidding.model.mapper.BiddingMapper">

	<insert id="insertBidding" parameterType="Bidding">
		<selectKey keyProperty="bidNo"
					resultType="int"
					order="BEFORE">
			SELECT BID_SEQ.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO BIDDING
		(BID_NO, UNVEILING_NO, MEMBER_NO, BID_PRICE, BID_DATE)
		VALUES (#{bidNo}, #{unveilingNo}, #{memberNo}, #{bidPrice}, SYSDATE)
	</insert>

</mapper>